# DECISION LOG
Numbered, dated, permanent. **Do not relitigate a closed decision** — if
circumstances change, add a new D-xxx that supersedes it and mark the old
one SUPERSEDED. Never edit history.

| ID | Date | Decision | Rationale | Status |
|----|------|----------|-----------|--------|
| D-001 | 2026-08-17 | **Co-pilot mode is the default build method.** Genspark AI Developer is rationed to novel layouts and charts only (dashboards, quotation comparison matrix, stock ledger, DPR). Never spend credits on CRUD screens. | Cost evidence: shell ≈ 7,400 credits; rebrand + one partial file ≈ 2,500; ~10,000/month available. Remaining work is repetitive replication against a frozen pattern — the worst use of per-run credits and the best use of a co-pilot that holds the contracts in context. Also: most of the 2,500 was spent re-reading context the tool loses every run, a cost that would recur on all 13 remaining modules. | ACTIVE |
| D-002 | 2026-08-17 | **Demo depth tiering.** Deep (list→form→detail→submit→approval→linked doc): Masters, Procurement, Inventory/Stores, Subcontractor, Project Controls/DPR. Medium (screens, no chained flow): Core Platform, Document Management, Plant & Fuel, HRMS/Labour/Expenses. Light (one screen or mock report): both Portals, Tally Export, Order Book MIS, Reports Catalogue. Dashboards built properly regardless of tier. | Equal depth across 15 modules would consume the schedule. Depth should follow risk — money, disputes and daily data entry. Management judges the system by dashboards. | ACTIVE |
| D-003 | 2026-08-17 | **Use the real client name and brand palette in the demo.** | Recognition helps a first-time ERP audience engage and give accurate requirements. Accepted risk: public unauthenticated URL, mitigated by fabricated data only + noindex/nofollow + no real financial or employee data. See Q-03. | ACTIVE |
| D-004 | 2026-08-19 | **The demo repo becomes the production repo via tag + clone.** Tag `demo-v1`, clone into a new private repo with full history, then additive commits only. Never rebuild from scratch. Demo repo stays live and frozen for continued client walkthroughs. | Preserves history, avoids a big-bang rewrite, and keeps a stable artefact for the client during the build. | ACTIVE |
| D-005 | 2026-08-19 | **Container/presenter split.** Page containers fetch; feature presenters receive props and never import `lib/data`. | The single highest-leverage carryover decision. Static export forces client-side fetching; production wants server components. Only the ~15-line container changes — the ~400-line presenter survives untouched. Without this, every screen is rewritten at handover. | ACTIVE |
| D-006 | 2026-08-19 | **Fixtures are future production seed data.** Fixture object field names must exactly match eventual Prisma model field names. | Reframes existing fixture work (60+ items with real IS/MoRTH specs, GSTIN-valid vendors, WBS hierarchies) as an asset rather than scaffolding. Any field-name divergence buys a mapper later, so naming discipline in `types.ts` is a production decision taken today. | ACTIVE |
| D-007 | 2026-08-19 | **Local dev environment, not GitHub Codespaces.** | Unlimited vs 120 core hours/month; no idle-burn concern; faster loop. Codespaces retained as a travel fallback. Firebase Studio is unavailable — new workspace creation and signups disabled 22 Jun 2026, full sunset 22 Mar 2027. | ACTIVE |
| D-008 | 2026-08-19 | **IDE is plain VS Code. Agentic/autonomous multi-file AI modes stay OFF.** Inline autocomplete is acceptable. Antigravity and similar agentic IDEs deliberately not adopted. | The project's primary technical risk is pattern drift across 15 modules built by a two-person team. A second AI editing files outside the review loop produces changes nobody logged and divergence between tracker and repo — a worse version of the problem that caused D-001. Also: no reliable current information on Antigravity's free-tier limits or data-handling terms, and this codebase will eventually hold real client commercial data. | ACTIVE |
| D-009 | 2026-08-19 | **Zod + react-hook-form + @hookform/resolvers approved** (Q-33, Q-34). One Zod schema per entity, reused unchanged in production for server-action input validation and Excel import row validation. | Hand-rolled validation in the demo would be discarded, then written three times in production (client form, server action, Excel import). Zod is the standard Prisma pairing and `z.infer<>` becomes the DTO source of truth. RHF avoids hand-rolled form state across 15 master screens. | ACTIVE |
| D-010 | 2026-08-19 | **Four production-safety rules apply from today**: R1 IDs are opaque (never parse/sort/display; show `code`). R2 No float money, no display units in the model (rupees; `Decimal(18,2)` in production; crore/lakh is formatting only). R3 Dates are ISO strings in all DTOs. R4 Common transactional fields from scope §22 exist on every document-like type from day one. | Each rule prevents a specific expensive rewrite. R4 makes the Prisma model a transcription rather than a redesign. See `02-ARCHITECTURE.md` §6. | ACTIVE |
| D-011 | 2026-08-20 | **`BrandLogo` renders real client artwork from `/public/brand/`, with an initials monogram fallback.** Single component consumed by both the sidebar and the login page. | One edit point for all brand surfaces. Fallback means a missing or renamed asset degrades to a readable monogram instead of a broken-image icon during a client walkthrough. | ACTIVE |
| D-012 | 2026-08-20 | **Login page is the first container/presenter reference implementation** under D-005. | Proves the pattern on a small screen before it is replicated across 15 modules. | SUPERSEDED by D-013 |
| D-013 | 2026-08-20 | **Login keeps the original single-column centred card. The split brand-panel redesign is rejected.** Client artwork is placed on light surfaces only. | The redesign was unrequested scope, its two-panel grid failed to split, and it used non-inverted foreground tokens on a navy surface producing unreadable text (DEF-014). The supplied PNG also has a baked-in white background, so it cannot sit on navy. Reverted; only the logo swap was kept. | ACTIVE |
| D-014 | 2026-08-21 | **ESLint enforces the theming and layering contract.** Three guards: (a) no raw Tailwind palette classes and no hex/rgb/hsla literals outside `config/theme.config.ts`; (b) no direct imports from `lib/data/adapters/**` anywhere; (c) no value imports of `@/lib/data` inside `features/**` (type-only imports permitted). | Makes D-005 and the theming promise machine-checked rather than remembered. Verified firing on a deliberate violation before adoption. Closes DEF-009. | ACTIVE |
| D-015 | 2026-08-21 | **Lint scope widened beyond Next's defaults** to `app components lib features config`. | `next lint` defaults omit `features/` and `config/` — the two directories the new rules most need to police. A silently unscanned rule is worse than no rule. | ACTIVE |
| D-016 | 2026-08-21 | **Fonts are self-hosted via `next/font/google`**; `themeConfig.typography.fontSans` references `var(--font-inter)`. No external font `<link>` tags. | Removes a render-blocking third-party request, works offline and under static export on Netlify, and clears the `no-page-custom-font` warning. Config remains the single source of truth for typography. | ACTIVE |
| D-017 | 2026-08-21 | **`next-env.d.ts` is tracked in git, not ignored.** | It supplies the CSS/image module declarations that `tsconfig.json` includes. Ignoring it made every fresh clone show TS2882 in the editor until someone ran a build — a defect that would have been inherited by the production repo under D-004. Closes DEF-017. | ACTIVE |
| D-018 | 2026-08-21 | docs/03-DESIGN-SYSTEM.md is the mandatory pre-read before writing any screen, and must be updated in the same commit as any component change. Components not yet transcribed are marked NOT TRANSCRIBED and must be read from source before first use. | Removes the main amnesia cost: re-deriving prop signatures every session. Marking gaps explicitly prevents invented APIs, which is how the four "API tensions" arose in the Genspark run. | ACTIVE |
| D-019 | 2026-08-21 | **`types.ts` extended additively only** — every new field on `Uom`, `HsnSac`, `Item` is optional, and `ItemGroup` stays a union with `ItemGroupDef` added alongside. | 60+ fixtures already satisfy the old shapes. A required field would break them all and cost more than it buys in a demo. Prisma will make them required where needed. | ACTIVE |
| D-020 | 2026-08-21 | **Fuel is an `Item` with `itemType: 'FUEL'`**, not a separate module. Its HSN carries `isNonGst: true`. | Answers part of Q-09 provisionally so Inventory can be built. HSD is outside GST (state VAT/excise) — the fixture's `gstRate: 0` was misleading. Reversible if the client's diesel handling differs. | ACTIVE |
| D-021 | 2026-08-21 | **Returnable materials are Items with `isReturnable: true`**, not Equipment records. | Answers Q-03 provisionally. Shuttering and staging are issued, tracked and recovered in quantity terms — inventory semantics, not asset semantics. Confirm with client. | ACTIVE |
| D-022 | 2026-08-21 | **Master list filters use a plain inline filter row, not `FilterBar`.** `FilterBar` is reserved for transaction screens (company/project/status/date). | `FilterState` is a closed shape with no room for domain filters (DEF-020). Forcing masters into it would mean widening a shared type for every module. Cheaper and clearer to let masters render two or three `SelectField`s inline. | ACTIVE |
| D-023 | 2026-08-21 | **Fixture edits are stored as patch records** (`<entity>.patch` in localStorage), merged over fixtures at read time. Newly created records live in `<entity>`. | Fixtures are static module imports and cannot be mutated. This gives the client a real edit experience in the demo without duplicating the whole dataset into storage. Disappears in production, where the DB is the single store. | ACTIVE |
| D-024 | 2026-08-21 | **UOM Master is the golden pattern**: `page.tsx` container + `features/<module>/<entity>/` holding `-screen.tsx`, `-columns.tsx`, `-schema.ts`. Every later master copies it. | Established once, replicated 13 times. Any change to the pattern must be made here first. | ACTIVE |
| D-025 | 2026-08-21 | **Zod v4 syntax is the project standard**; field-level messages use `{ message: '…' }`, not the removed `invalid_type_error`. `@hookform/resolvers` must be v5.x. | v4 was installed by `npm install zod`. Fixing the syntax once here is cheaper than pinning back, but the pairing must be recorded or every later schema repeats the error. | ACTIVE |
| D-026 | 2026-08-21 | **The presenter import ban targets `@/lib/data`, `@/lib/data/index` and `@/lib/data/store` only. `@/lib/data/types` is explicitly permitted.** | Base ESLint's `no-restricted-imports` cannot distinguish `import type` from a value import, and adding `@typescript-eslint` purely for `allowTypeImports` is not worth a new dependency. Presenters legitimately need domain types; they must not reach the fetch or persistence layers. | ACTIVE |
| D-027 | 2026-08-21 | **Presenter import restrictions use `paths` (exact specifier), not `patterns`.** Adapter restrictions keep using `patterns` with both `/*` and `/**` forms. | ESLint 8 matches `patterns.group` with gitignore semantics, so `@/lib/data` silently also matched `@/lib/data/types` and blocked legitimate type imports. `paths` is exact-match and cannot over-reach. Supersedes the mechanism (not the intent) of D-014(c) and D-026. | ACTIVE |


## Superseded decisions
| ID | Superseded by | Note |
|----|---------------|------|
| D-012 | D-013 | Login presenter was reverted; the container/presenter pattern is unaffected and is still first exercised at UOM Master (Step 5). |

